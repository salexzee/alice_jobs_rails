- if @jobs.empty?
	%h1.text-center Sorry, there are no jobs to view.
- else
	- @jobs.each do |job|
		%h2= link_to job.title, job, class: 'text-primary'
		%p.text-success= job.company
		%hr

	= will_paginate @jobs, inner_window: 1, outer_window: 0